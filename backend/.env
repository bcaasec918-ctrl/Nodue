const BASE_URL = 'http://localhost:3001';

export const api = {
  getStudents: async () => {
    const res = await fetch(`${BASE_URL}/students`);
    return await res.json();
  },

  addAttendance: async (record) => {
    await fetch(`${BASE_URL}/attendance`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify(record)
    });
  },

  getFees: async (studentId: string) => {
    const res = await fetch(`${BASE_URL}/fees/${studentId}`);
    return await res.json();
  }
};
DATABASE_URL='postgresql://neondb_owner:npg_B1Cogdbak3up@ep-tiny-bar-a1tdby49-pooler.ap-southeast-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require'
PORT=3001
